package com.shinkatech.renshugo.ui.theme

import androidx.compose.ui.graphics.Color

// ---------- Brand Colors ----------

val NavyPrimary = Color(0xFF1A2A4A)
val PrimaryBlue = Color(0xFF5B8DEF)
val AccentCyan = Color(0xFF00C2E0)
val DeepBlue = Color(0xFF3A6AD4)

val LightBlueBackground = Color(0xFFF0F4FF)
val MutedBlueText = Color(0xFF6B89B8)

val SoftCircleStroke = Color(0xFFB8C8E8)
val DotShadowBlue = Color(0xFFC5D5F0)

// ---------- Light Theme ----------

val LightPrimary = PrimaryBlue
val LightOnPrimary = Color.White

val LightSecondary = AccentCyan
val LightOnSecondary = Color.White

val LightBackground = LightBlueBackground
val LightSurface = Color.White

val LightOnBackground = NavyPrimary
val LightOnSurface = NavyPrimary

val LightOutline = SoftCircleStroke

// ---------- Dark Theme ----------

val DarkBackground = Color(0xFF0D1B2E)
val DarkSurface = Color(0xFF13233A)

val DarkPrimary = PrimaryBlue
val DarkSecondary = AccentCyan

val DarkOnPrimary = Color.White
val DarkOnSecondary = Color.Black

val DarkOnBackground = Color(0xFFEAF2FF)
val DarkOnSurface = Color(0xFFEAF2FF)

val DarkOutline = Color(0xFF3B527A)